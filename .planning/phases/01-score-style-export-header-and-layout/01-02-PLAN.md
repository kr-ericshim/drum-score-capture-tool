---
phase: 01-score-style-export-header-and-layout
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01-PLAN.md"
files_modified:
  - backend/app/pipeline/export.py
  - backend/tests/test_review_export_refinalization.py
  - backend/tests/test_export_document_header.py
autonomous: true
requirements:
  - EXP-03
  - EXP-04
must_haves:
  truths:
    - "Only the PDF path gets the score-style header; PNG/JPG page-image exports stay unchanged."
    - "The first-page title band consumes real layout space and never overlays or clips the music image."
    - "Page diagnostics stay about score content, not about the newly added header band."
  artifacts:
    - path: backend/app/pipeline/export.py
      provides: "shared first-page PDF header composition, font resolution, and diagnostics-safe PDF assembly"
    - path: backend/tests/test_export_document_header.py
      provides: "focused proof for first-page-only header rendering, blank-row hiding, and review-export parity"
    - path: backend/tests/test_review_export_refinalization.py
      provides: "regression coverage that review export still preserves page identity and preview behavior after header composition"
  key_links:
    - from: backend/app/pipeline/export.py
      to: backend/app/main.py
      via: "shared `document_header` consumption in both export entrypoints"
      pattern: "export_frames|export_selected_pages|document_header"
    - from: backend/app/pipeline/export.py
      to: backend/tests/test_export_document_header.py
      via: "title-band composition, font fallback, and page-diagnostics invariants"
      pattern: "_compose_pdf_pages_with_document_header|_diagnose_page_image"
---

<objective>
Turn the PDF into a document-grade score export without destabilizing print layout or review diagnostics.

Purpose: This plan adds one shared PDF header compositor for the first page only and keeps diagnostics, page sizing, and review-export parity trustworthy afterward.
Output: a score-style PDF title band, deterministic font fallback behavior, and regression coverage proving page 1 gains the header while the rest of the export contract stays stable.
</objective>

<execution_context>
@/Users/ericshim/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ericshim/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-score-style-export-header-and-layout/01-CONTEXT.md
@.planning/phases/01-score-style-export-header-and-layout/01-RESEARCH.md
@.planning/phases/01-score-style-export-header-and-layout/01-VALIDATION.md
@.planning/phases/01-score-style-export-header-and-layout/01-PATTERNS.md
@backend/app/pipeline/export.py
@backend/app/main.py
@backend/tests/test_review_export_refinalization.py
@backend/tests/test_sheet_finalize.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add one shared first-page PDF header composer with deterministic font fallback</name>
  <read_first>
.planning/phases/01-score-style-export-header-and-layout/01-CONTEXT.md
.planning/phases/01-score-style-export-header-and-layout/01-RESEARCH.md
.planning/phases/01-score-style-export-header-and-layout/01-UI-SPEC.md
.planning/phases/01-score-style-export-header-and-layout/01-PATTERNS.md
backend/app/pipeline/export.py
backend/tests/test_review_export_refinalization.py
  </read_first>
  <files>
backend/app/pipeline/export.py
backend/tests/test_export_document_header.py
  </files>
  <behavior>
    - `export_frames(...)` and `export_selected_pages(...)` both route PDF pages through one shared helper before `_prepare_pdf_image(...)`.
    - Page 1 gets a real header band with centered title hierarchy plus optional performer/BPM/date/memo rows; blank optional values hide their row completely.
    - PNG/JPG output files remain raw finalized page images and do not inherit the PDF header.
  </behavior>
  <action>
    Build the document-grade PDF compositor inside the existing Pillow path.
    - Add helpers in `backend/app/pipeline/export.py` such as `_compose_pdf_pages_with_document_header(...)`, `_render_document_header_band(...)`, and `_resolve_score_header_font(...)`.
    - Resolve fonts deterministically from explicit OS paths before falling back: macOS `/System/Library/Fonts/AppleSDGothicNeo.ttc`, Windows `%WINDIR%/Fonts/malgun.ttf`, then any safe CJK-capable fallback path already discoverable on the machine.
    - Limit header growth with explicit wrapping and line caps so long `title` or `memo` values cannot create a comically tall first page or collide with the music body.
  </action>
  <acceptance_criteria>
    - `rg -n "_compose_pdf_pages_with_document_header|_render_document_header_band|_resolve_score_header_font" backend/app/pipeline/export.py` succeeds.
    - `backend/app/pipeline/export.py` applies the header only when `"pdf"` is requested and `document_header.title` is non-empty.
    - `backend/tests/test_export_document_header.py` exists and covers first-page-only header rendering, blank-row hiding, and PNG/PDF divergence.
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=backend python -m unittest backend.tests.test_export_document_header</automated>
  </verify>
  <done>The PDF path now has one shared score-style header compositor instead of duplicated or overlay-based rendering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep diagnostics, page sizing, and review-export parity honest after the title band exists</name>
  <read_first>
.planning/phases/01-score-style-export-header-and-layout/01-RESEARCH.md
.planning/phases/01-score-style-export-header-and-layout/01-VALIDATION.md
.planning/phases/01-score-style-export-header-and-layout/01-PATTERNS.md
backend/app/pipeline/export.py
backend/tests/test_review_export_refinalization.py
backend/tests/test_sheet_finalize.py
  </read_first>
  <files>
backend/app/pipeline/export.py
backend/tests/test_review_export_refinalization.py
backend/tests/test_export_document_header.py
  </files>
  <behavior>
    - `page_diagnostics` on page 1 still reflect the score content and do not flip to suspicious just because the header band exists.
    - Review-export PDFs use the same header composer and keep preview-image behavior, page counts, and selected-page identity stable.
    - First-page composition preserves print-ready page geometry instead of shrinking or stretching later pages to compensate for the title band.
  </behavior>
  <action>
    Protect the existing export invariants around the new title band.
    - Compute page diagnostics from the finalized score page before the header band is composited, or explicitly mask the header band region for page 1 diagnostics.
    - Reuse the same PDF composition helper in both export entrypoints so review export cannot drift from initial export.
    - Extend parity tests to prove preview-image generation, page count, and review-export refinalization still hold after the header work lands.
  </action>
  <acceptance_criteria>
    - `backend/tests/test_export_document_header.py` proves page 1 diagnostics do not become suspicious solely because the document header exists.
    - `backend/tests/test_review_export_refinalization.py` still proves selected pages stay individual pages and PDF-only output still writes preview images.
    - `PYTHONPATH=backend python -m unittest backend.tests.test_review_export_refinalization backend.tests.test_export_document_header` exits with code `0`.
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=backend python -m unittest backend.tests.test_review_export_refinalization backend.tests.test_export_document_header</automated>
    <automated>PYTHONPATH=backend python -m unittest backend.tests.test_sheet_finalize</automated>
  </verify>
  <done>The PDF header now coexists with honest diagnostics and unchanged review-export geometry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-entered document metadata -> Pillow drawing and font lookup | long or multilingual strings can overflow layout or fail on a platform-specific font path |
| finalized score pages -> page diagnostics and review UI | the new title band can contaminate top-edge heuristics and make legitimate pages look suspicious |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | D | `backend/app/pipeline/export.py` header compositor | mitigate | Cap line count and wrap width for `title`/`memo`, resolve fonts through an explicit fallback chain, and only compose the header on the PDF path so oversized text cannot destabilize every export format. |
| T-02-02 | T | `backend/app/pipeline/export.py` diagnostics flow | mitigate | Keep `page_diagnostics` based on finalized score content before header composition, and add regression tests proving the title band does not create false suspicious-page signals. |
</threat_model>

<verification>
<automated>PYTHONPATH=backend python -m unittest backend.tests.test_sheet_finalize backend.tests.test_review_export_refinalization backend.tests.test_export_document_header</automated>
</verification>

<success_criteria>
- Exported PDFs gain a first-page score-style header without affecting PNG/JPG outputs.
- The title band uses real layout space and keeps print-ready page geometry stable.
- Diagnostics and review export still describe the score content truthfully after header composition.
</success_criteria>

<output>
After completion, create `.planning/phases/01-score-style-export-header-and-layout/01-02-SUMMARY.md`
</output>
