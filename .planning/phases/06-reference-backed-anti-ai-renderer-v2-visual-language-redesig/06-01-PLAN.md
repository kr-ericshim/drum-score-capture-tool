---
phase: 06-reference-backed-anti-ai-renderer-v2-visual-language-redesig
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - desktop/renderer-v2/src/styles/tokens.css
  - desktop/renderer-v2/src/styles/components.css
  - desktop/renderer-v2/src/lib/i18n.js
  - desktop/renderer-v2/src/tests/stitch-fidelity.test.js
  - desktop/renderer-v2/src/tests/i18n.test.js
autonomous: true
requirements:
  - REL-03
must_haves:
  truths:
    - "renderer-v2의 기본 톤은 amber/brown monotone workbench가 아니라 neutral dark shell + sparse warm accent로 읽혀야 한다."
    - "mono/uppercase microcopy는 기본 shell 문체가 아니라 제한적 보조 표기여야 한다."
    - "user-facing shell vocabulary는 generic tool frame이 아니라 score capture task semantics를 우선해야 한다."
  artifacts:
    - path: desktop/renderer-v2/src/styles/tokens.css
      provides: "Phase 6 전체의 색상/표면/타이포 기조를 결정하는 토큰 기준"
    - path: desktop/renderer-v2/src/styles/components.css
      provides: "top bar, rail, cards, buttons, helper text의 실제 house style"
    - path: desktop/renderer-v2/src/lib/i18n.js
      provides: "shell과 step surface가 공유하는 copy truth"
    - path: desktop/renderer-v2/src/tests/stitch-fidelity.test.js
      provides: "새 visual language가 generic workbench로 되돌아가지 않게 막는 구조/문구 회귀 가드"
  key_links:
    - from: desktop/renderer-v2/src/styles/tokens.css
      to: desktop/renderer-v2/src/styles/components.css
      via: "shared palette, paper exception, accent budget"
      pattern: "--bg-*|--accent|--surface-paper"
    - from: desktop/renderer-v2/src/lib/i18n.js
      to: desktop/renderer-v2/src/tests/i18n.test.js
      via: "task-first vocabulary and bilingual copy parity"
      pattern: "topbar|rail|lane|source|roi|review|export"
---

<objective>
Reset renderer-v2의 공용 visual language를 calm한 desktop tool 기준으로 다시 고정한다.

Purpose: current monotone workbench tone을 shared tokens/components/i18n স্তরে 끊어내서 이후 shell/screen 작업이 새 house style 위에서 진행되게 한다.
Output: neutral shell palette, restrained accent budget, calmer label hierarchy, and task-first copy guardrails.
</objective>

<execution_context>
@/Users/ericshim/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ericshim/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-reference-backed-anti-ai-renderer-v2-visual-language-redesig/06-RESEARCH.md
@.planning/phases/06-reference-backed-anti-ai-renderer-v2-visual-language-redesig/06-UI-SPEC.md
@.planning/phases/06-reference-backed-anti-ai-renderer-v2-visual-language-redesig/06-VALIDATION.md
@.planning/phases/06-reference-backed-anti-ai-renderer-v2-visual-language-redesig/06-PATTERNS.md
@desktop/renderer-v2/src/styles/tokens.css
@desktop/renderer-v2/src/styles/components.css
@desktop/renderer-v2/src/lib/i18n.js
@desktop/renderer-v2/src/tests/stitch-fidelity.test.js
@desktop/renderer-v2/src/tests/i18n.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebase shared shell tokens onto neutral dark surfaces with sparse warm accent</name>
  <read_first>
.planning/phases/06-reference-backed-anti-ai-renderer-v2-visual-language-redesig/06-RESEARCH.md
.planning/phases/06-reference-backed-anti-ai-renderer-v2-visual-language-redesig/06-UI-SPEC.md
.planning/phases/06-reference-backed-anti-ai-renderer-v2-visual-language-redesig/06-PATTERNS.md
desktop/renderer-v2/src/styles/tokens.css
desktop/renderer-v2/src/styles/components.css
  </read_first>
  <files>
desktop/renderer-v2/src/styles/tokens.css
desktop/renderer-v2/src/styles/components.css
  </files>
  <behavior>
    - shell dominant/secondary surfaces read as neutral graphite layers rather than a single amber slab.
    - accent is spent only on active step, one primary CTA, current selection, and focus-visible states.
    - shared helper labels and rail headers stop defaulting to machine-like mono/uppercase styling.
  </behavior>
  <action>
    Reset the shared renderer-v2 house style instead of recoloring one screen at a time.
    - In `tokens.css`, move the base shell colors toward the UI-SPEC values around `#121418` and `#1b1f26`, preserve a warm accent near `#d7a347`, and keep paper/document as a separate exception surface instead of a shell default.
    - In `components.css`, reduce the amount of selectors that inherit mono + uppercase + micro-font defaults, especially shared shell labels like `.topbar-copy span`, `.engine-badge`, `.rail-head p`, and `.rail-footer-label`.
    - Keep readable contrast for existing accessibility scaffolding and focus treatments; do not darken the UI by simply lowering all text contrast.
  </action>
  <acceptance_criteria>
    - `desktop/renderer-v2/src/styles/tokens.css` contains the Phase 6 shell baseline values `#121418`, `#1b1f26`, and `#d7a347`.
    - `desktop/renderer-v2/src/styles/components.css` no longer applies mono/uppercase styling as the default tone for the top-bar secondary line, engine badge, and rail labels at the same time.
    - `rg -n "#121418|#1b1f26|#d7a347" desktop/renderer-v2/src/styles/tokens.css` exits with code `0`.
  </acceptance_criteria>
  <verify>
    <automated>cd desktop && node --test renderer-v2/src/tests/stitch-fidelity.test.js && npm run check:renderer-v2</automated>
  </verify>
  <done>공용 palette와 shared chrome tone이 Phase 6 contract에 맞게 다시 고정된다.</done>
</task>

<task type="auto">
  <name>Task 2: Replace generic shell vocabulary with task-first bilingual copy and lock it in tests</name>
  <read_first>
.planning/phases/06-reference-backed-anti-ai-renderer-v2-visual-language-redesig/06-RESEARCH.md
.planning/phases/06-reference-backed-anti-ai-renderer-v2-visual-language-redesig/06-UI-SPEC.md
.planning/phases/06-reference-backed-anti-ai-renderer-v2-visual-language-redesig/06-PATTERNS.md
desktop/renderer-v2/src/lib/i18n.js
desktop/renderer-v2/src/tests/stitch-fidelity.test.js
desktop/renderer-v2/src/tests/i18n.test.js
  </read_first>
  <files>
desktop/renderer-v2/src/lib/i18n.js
desktop/renderer-v2/src/tests/stitch-fidelity.test.js
desktop/renderer-v2/src/tests/i18n.test.js
  </files>
  <behavior>
    - primary shell wording describes current task, not the tool frame.
    - Korean and English labels stay intent-matched without machine-style strings like `ENGINE_READY` or `WORKBENCH STATUS`.
    - future regressions toward generic AI/workbench language fail fast in tests.
  </behavior>
  <action>
    Rewrite the shared renderer-v2 copy baseline around user intent.
    - Update `i18n.js` keys used by top bar, rail, lane, and screen helpers so they prefer score-capture task language such as selecting video, capture region, results review, output summary, and next step.
    - Remove or rewrite generic frame-first phrases documented as disallowed in `06-UI-SPEC.md`, including shell labels that sound like system dashboards rather than a local music tool.
    - Extend `stitch-fidelity.test.js` and `i18n.test.js` so the most important disallowed vocabulary is asserted against directly.
  </action>
  <acceptance_criteria>
    - `desktop/renderer-v2/src/lib/i18n.js` no longer uses generic machine-style strings for the primary shell labels that Phase 6 marked as disallowed.
    - `desktop/renderer-v2/src/tests/stitch-fidelity.test.js` contains assertions against generic workbench vocabulary on the shell surfaces.
    - `desktop/renderer-v2/src/tests/i18n.test.js` proves the new shell language stays Korean-first with English fallback parity.
  </acceptance_criteria>
  <verify>
    <automated>cd desktop && node --test renderer-v2/src/tests/stitch-fidelity.test.js renderer-v2/src/tests/i18n.test.js</automated>
  </verify>
  <done>renderer-v2의 공용 copy baseline이 task-first language로 고정되고 회귀 테스트로 보호된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared shell tokens -> every renderer-v2 screen | a bad base palette or text hierarchy can make the whole product read like a generic generated workbench even when screen-level structure is good |
| i18n copy registry -> shell renderers | stale generic vocabulary can reintroduce misleading AI-tool framing without any structural code bug |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T | `desktop/renderer-v2/src/styles/tokens.css` / `components.css` | mitigate | reset the shell to neutral-dark layers with constrained accent usage so one warm tint cannot flatten every surface into the same material |
| T-06-02 | I | `desktop/renderer-v2/src/lib/i18n.js` | mitigate | replace frame-first vocabulary with task-first labels and pin the change with explicit string assertions in renderer-v2 tests |
</threat_model>

<verification>
<automated>cd desktop && node --test renderer-v2/src/tests/stitch-fidelity.test.js renderer-v2/src/tests/i18n.test.js</automated>
<automated>cd desktop && npm run check:renderer-v2</automated>
</verification>

<success_criteria>
- shared tokens no longer push a uniform amber/brown workbench across every shell surface
- shared shell labels read like score-capture tasks rather than system chrome
- regression tests fail if the generic workbench vocabulary or tone returns
</success_criteria>

<output>
After completion, create `.planning/phases/06-reference-backed-anti-ai-renderer-v2-visual-language-redesig/06-01-SUMMARY.md`
</output>
